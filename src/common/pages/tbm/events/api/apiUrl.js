const base = '/tm/'

// 删除事件
const BATCH_DELETE_EVENT = `${base}news/popFeel/batchDelete`

const CHECK_DATE = `${base}event/getEventDateType`

const CHECK_SIMILAR = `${base}event/checkSimilarEvent`

export default {
    BATCH_DELETE_EVENT,
    CHECK_DATE,
    CHECK_SIMILAR
}