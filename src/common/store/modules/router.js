const state = {
}

// getters 获取state
const getters = {

}

// actions 外部调用action
// 例store.dispatch('changeNavKeyList')
const actions = {
  
}

// mutations 更改state
const mutations = {
   
}

export default {
    namespaced: true,
    state,
    getters,
    actions,
    mutations
}